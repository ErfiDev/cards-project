package main

import "fmt"

func main() {
	// cards := newDeck()
	// cards.print()
	// deal(cards, 10)
	condition := true;
	var i int = 0;
	for condition {
		i++
		fmt.Println(i)
		if i == 15 {
			break
		}
	}


	// newTenCar := createRandomCar(10);
	// newTenCar.print()


	// newMap := make(map[string]bool);
	// newMap["single?"] = true;
	// fmt.Printf("\n %v \n" , newMap);
}
